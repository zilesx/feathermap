services:
  cloudflared:
    image: cloudflare/cloudflared:latest
    container_name: flyway-cloudflared
    restart: unless-stopped
    command:
      - tunnel
      - --no-autoupdate
      - run
      - --token
      - ${TUNNEL_TOKEN}
    depends_on:
      flyway-api:
        condition: service_healthy
